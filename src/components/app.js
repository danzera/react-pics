import React from 'react';
import SearchBar from './SearchBar';
import '../css/app.css';

const App = _ => {
	return (
		<div className="app ui container">
			<SearchBar />
		</div>
	);
}

export default App;
